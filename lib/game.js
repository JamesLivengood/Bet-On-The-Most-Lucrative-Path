
class Game {
  contructor(grid, dfsBuildMaze, bfsMazeSolver) {
    this.grid = grid;
    this.dfsBuildMaze = dfsBuildMaze;
    this.bfsMazeSolver = bfsMazeSolver;
  }

  // $('img.yin-yang')[0].className = 'yin-yang-spinning';
      // $('img.yin-yang-spinning')[0].className = 'yin-yang';

}

export default Game;
